import styled from "styled-components";

const HeaderStyled = styled.header`
  .header {
    max-width: 350px;
    display: flex;

    &-hidden {
      display: none;
    }
  }
`;

export default HeaderStyled;
